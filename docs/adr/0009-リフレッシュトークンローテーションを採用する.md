# ADR-0009: リフレッシュトークンローテーションを採用する

| 項目       | 内容       |
| ---------- | ---------- |
| ステータス | Accepted   |
| 日付       | 2026-08-04 |

---

## コンテキスト

従来の認証では有効期限1時間のJWTのみを発行しており、期限切れ後は再ログインが必要だった。
ブラウザ利用時の利便性を改善しつつ、長期間有効な認証情報の漏洩リスクを抑える必要がある。

本システムはCookie認証、OAuthログイン、CSRF保護、JWTブラックリストを既に採用している。
また、認証セッションはRedisの一時的な可用性に左右されず、再起動後も維持する必要がある。

## 決定

アクセストークンには有効期限10分のJWTを使用し、リフレッシュトークンには有効期限30日の不透明なランダム値を使用する。
リフレッシュトークンのSHA-256ハッシュをPostgreSQLへ保存し、使用ごとにローテーションする。
消費から30秒以内の再提示は正規クライアントの並行更新として扱い、HTTP 409を返してセッション系列を維持する。
30秒を超えた使用済みトークンの再利用を検知した場合は、同じ`family_id`に属するセッションをすべて失効させる。

リフレッシュAPIは通常時にIP単位30回/分のRedisレートリミットを適用するが、Redis障害時はfail-openとする。
セッションの有効性と失効状態はPostgreSQLで必ず検証する。

## 理由

アクセストークンを短命化することで、JWT漏洩時の有効期間を制限できる。
リフレッシュトークンをサーバー管理することで、ログアウトや盗難検知時に即時失効できる。

不透明なランダム値を使用することで、リフレッシュトークン自体にユーザー情報を含めず、署名鍵とも分離できる。
DBにはハッシュだけを保存するため、データベースが漏洩しても保存値をそのまま認証へ利用できない。

PostgreSQLは既存の永続化基盤であり、トランザクションと行ロックを利用してトークン交換を一度だけ成功させられる。
Redis障害や再起動によって全ユーザーのログイン状態が失われることも避けられる。

SPAではアクセストークン期限切れ時に複数のAPI呼び出しが同時に更新を要求する可能性がある。
30秒の猶予期間により、この通常の競合をトークン盗難と誤検知して正規セッションを失効させることを防ぐ。
競合時はCookieを削除しないため、先に成功したレスポンスが設定した後継トークンを継続利用できる。

リフレッシュトークンは256ビットの暗号論的乱数であり、パスワードと異なり総当たりが現実的ではない。
Redis障害時もPostgreSQLでトークンを検証することで、セキュリティ境界を維持しながら既存セッションの可用性を優先する。

## 代替案

| 代替案 | 不採用の理由 |
| ------ | ------------ |
| リフレッシュトークンにもJWTを使用する | サーバー側での個別失効や再利用検知に結局状態管理が必要になり、JWTである利点が小さい |
| Redisへ保存する | Redis障害やデータ消失時に全セッションが失われる可能性がある |
| 固定リフレッシュトークンを有効期限まで再利用する | 盗難後も攻撃者が繰り返しアクセストークンを発行できる |
| アクセストークンを長寿命のまま維持する | JWT漏洩時の被害期間が長くなる |
| 消費済みトークンを猶予なく盗難と判定する | SPAの並行更新やネットワーク再送で正規セッションを失効させる |
| リフレッシュAPIをRedis障害時にfail-closedとする | アクセストークン失効後は全ユーザーが更新できず、障害の影響が10分で顕在化する |

## 影響

### ポジティブな影響

- アクセストークン漏洩時の被害期間が10分に制限される
- ユーザーは30日間、再ログインせずセッションを更新できる
- ログアウト時にリフレッシュセッションを即時失効できる
- 使用済みトークンの再利用から盗難を検知できる
- 並行更新や短時間の再送で正規セッションが強制失効しない
- Redis障害中もPostgreSQLが利用可能なら既存セッションを更新できる
- DB上に平文のリフレッシュトークンを残さない

### ネガティブな影響・トレードオフ

- 認証更新ごとにPostgreSQLアクセスが発生する
- 同時に複数の更新要求が送信された場合、一方だけが成功し、残りは409になる
- フロントエンド側で更新要求を単一化する必要がある
- 30秒の猶予期間内に攻撃者が先にトークンを使用した場合、正規クライアントの競合だけでは系列失効を発動できない
- Redis障害時はリフレッシュAPIのIPレートリミットが無効になり、PostgreSQLへのDoS耐性が低下する
- 期限切れセッションを定期削除する運用が必要になる
- PostgreSQL障害中はトークン更新とサーバー側の完全なログアウトが利用できないが、JWT失効とCookie削除は試行する

## 関連ADR

- [ADR-0003](0003-postgresql-の採用.md): PostgreSQLの採用
- [ADR-0008](0008-レートリミット障害時のfail-open-fail-closed方針.md): 障害時のセキュリティ方針
