meta {
  name: Signup
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/v1/signup
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{email}}",
    "password": "{{password}}"
  }
}
