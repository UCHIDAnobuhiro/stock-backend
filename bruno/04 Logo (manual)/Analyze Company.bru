meta {
  name: Analyze Company
  type: http
  seq: 2
  tags: [
    manual
  ]
}

post {
  url: {{baseUrl}}/v1/logo/analyze
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "company_name": "{{companyName}}"
  }
}
